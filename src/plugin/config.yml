## YAML Template.
---
package:
  name: dashboard
  description: The dashboard panel and associated widgets
  type: iframe
services:
  routes:
    - path: "{{plugin}}"
      view: main
      authorization: true
  menu:
    items:
      - name: dashboard
        path: "{{plugin}}"
        label: Dashboard
        icon: dashboard
