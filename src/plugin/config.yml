## YAML Template.
---
package:
  name: dashboard
  description: The dashboard panel and associated widgets
  type: iframe
install:
  routes:
    - path: dashboard
      view: main
      authorization: true
  menu:
    - name: dashboard
      definition:
        path: ["dashboard"]
        label: Dashboard
        icon: dashboard
